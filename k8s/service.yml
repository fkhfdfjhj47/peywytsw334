apiVersion: v1
kind: Service
metadata:
  name: Fdsggsga
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "Fdsggsga"
    port: 8080
  selector:
    app: Fdsggsga
